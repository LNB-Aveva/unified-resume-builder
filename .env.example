# Root .env.example — see the per-directory files for complete setup:
#   backend/.env.example  → copy to backend/.env
#   frontend/.env.example → copy to frontend/.env.local
#
# This file is kept for backwards compatibility. The per-directory
# files are authoritative and have inline documentation.

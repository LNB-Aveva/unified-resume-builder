# ============================
# Web Framework
# ============================
# FastAPI = The framework that handles incoming web requests (like when a user
#           clicks "Analyze My Resume" on the website, FastAPI receives that request)
fastapi==0.139.2
# Uvicorn = The server that actually RUNS FastAPI (FastAPI is the app, Uvicorn is the engine)
uvicorn==0.32.0
# python-multipart = Lets users upload files (like their resume PDF)
python-multipart==0.0.31

# ============================
# Data Handling
# ============================
# Pydantic = Validates data shapes (ensures a resume has the right fields)
pydantic>=2.13.0
# python-dotenv = Loads your .env file so you can use API keys without hardcoding them
python-dotenv==1.2.2

# ============================
# PDF Generation
# ============================
# WeasyPrint was originally planned but requires libgobject/libpango (GTK)
# which is not available on Windows dev or free-tier Linux hosts without
# extra system packages. Replaced with fpdf2 (pure Python, no system deps).
fpdf2==2.8.7

# ============================
# Testing
# ============================
# pytest = The testing framework - lets you write and run automated tests
pytest==9.0.3
# httpx = Makes HTTP requests in tests (simulates a user calling your API)
httpx==0.28.0

# ============================
# Rate Limiting
# ============================
slowapi==0.1.9

# ============================
# Development Tools
# ============================
# black = Automatically formats your Python code to look clean and consistent
black==26.3.1
# ruff = Fast linter that catches bugs and style issues before they become problems
ruff==0.12.2
# bandit = Security linter that finds common vulnerabilities in Python code
bandit==1.8.3
# detect-secrets = Scans for accidentally committed credentials/API keys
detect-secrets==1.5.0
# pytest-cov = Test coverage reporting (shows which lines are tested)
pytest-cov==7.1.0
# hypothesis = Property-based testing (generates random inputs to find edge cases)
hypothesis==6.156.9
# mypy = Static type checker for Python
mypy==2.3.0
# semgrep = Advanced pattern-based security scanner
semgrep==1.170.0
# pip-audit = Scans installed packages for known CVEs (vulnerability checker)
pip-audit==2.10.1

# ============================
# Security: pin transitive deps to patched versions
# ============================
# Semgrep and other tools pull in these packages; pin to versions that clear pip-audit.
# Unfixable CVEs (pip itself, scapy via semgrep, cryptography 48.x jump) are
# --ignore-vuln'd in ci.yml instead.
requests>=2.33.0
urllib3>=2.7.0
idna>=3.15
pygments>=2.20.0
filelock>=3.20.3
cryptography>=46.0.7

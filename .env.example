# Environment variables are split by service.
# See the authoritative sources:
#
#   backend/.env.example   — FastAPI backend (HuggingFace, Supabase, JWT, server settings)
#   frontend/.env.example  — Next.js frontend (API URL, Supabase public keys, Sentry)

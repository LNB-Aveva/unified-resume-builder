---
name: deploy
description: Build, commit, push, and verify deployment to Vercel
---

# Deploy to Production

1. Run `npm run build` in `frontend/` to verify no errors
2. Stage changed files (never use `git add -A`)
3. Commit with a short, casual, human-written message (no Co-Authored-By, no emojis)
4. Verify git email is `274821620+LNB-Aveva@users.noreply.github.com`
5. Push to origin
6. Report the deployed URL: https://resumeai.cv
